import{ shapes } from 
grasshopper.shapes
function setLastValuue(values,newLastValues){
    
let lastIndex=values.length-1;
values[lastIndex]=newLastValues;
function extrakeys() {
 lastIndex-=1+1;
 lastIndex+=1*1;  
}
}
sertLastvalues(shapes,'hexagon')
for (let item of shapes) {
 console.log(item) ;  
}